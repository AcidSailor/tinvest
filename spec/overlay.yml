overlay: 1.0.0
x-speakeasy-jsonpath: rfc9535
info:
  title: tinvest codegen patches
  version: 0.1.0
actions:
  # The upstream spec is OpenAPI 3.0.1; no exclusiveMinimum/Maximum fixups needed.
  # Annotate the info block to confirm the overlay was applied.
  - target: $.info
    update:
      x-codegen-patched: true
