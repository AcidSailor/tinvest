version: "2"

linters:
  default: standard
  enable:
    - modernize

formatters:
  enable:
    - gofumpt
    - golines
  settings:
    gofumpt:
      extra:
        group-params: true
    golines:
      max-len: 80
